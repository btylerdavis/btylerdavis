   border-radius: 44px;
      background:
        radial-gradient(circle at 90% 10%, rgba(98,204,231,0.38) 0 120px, transparent 121px),
        #ffe7e1;
      box-shadow: 10px 11px 0 var(--brown);
      position: relative;
      overflow: hidden;
    }

    .gift-card h2 {
      max-width: 690px;
      margin-bottom: 20px;
      font-size: clamp(3rem, 5vw, 5rem);
    }

    .gift-card p {
      max-width: 650px;
      color: rgba(48,37,31,0.72);
      font-size: 1.1rem;
    }

    .gift-actions { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 26px; }

    .gift-visual {
      min-height: 330px;
      position: relative;
      display: grid;
      place-items: center;
    }

    .gift-box {
      width: 235px;
      height: 200px;
      border: 3px solid var(--brown);
      border-radius: 24px;
      background: var(--banana);
      box-shadow: 10px 10px 0 var(--brown);
      position: relative;
      transform: rotate(3deg);
    }

    .gift-box::before {
      content: "";
      position: absolute;
      left: -14px;
      right: -14px;
      top: -37px;
      height: 58px;
      border: 3px solid var(--brown);
      border-radius: 22px;
      background: #fff7d8;
    }

    .gift-ribbon-v,
    .gift-ribbon-h {
      position: absolute;
      background: var(--coral);
      border: 2px solid var(--brown);
    }

    .gift-ribbon-v { width: 38px; top: -39px; bottom: -3px; left: 99px; }
    .gift-ribbon-h { height: 34px; left: -3px; right: -3px; top: 78px; }

    .gift-tag {
      position: absolute;
      right: -39px;
      top: 18px;
      padding: 11px 15px;
      border: 2px solid var(--brown);
      border-radius: 12px;
      background: white;
      box-shadow: 4px 4px 0 var(--brown);
      font-family: var(--font-display);
      font-weight: 950;
      transform: rotate(9deg);
    }

    .faq-wrap {
      display: grid;
      grid-template-columns: 0.8fr 1.2fr;
      gap: 60px;
      align-items: start;
    }

    .faq-intro { position: sticky; top: 30px; }

    .faq-intro h2 {
      margin-bottom: 18px;
      font-size: clamp(3rem, 5vw, 4.8rem);
    }

    .faq-intro p { color: rgba(48,37,31,0.68); }

    .faq-list { border-top: 2px solid var(--brown); }

    details { border-bottom: 2px solid var(--brown); }

    summary {
      list-style: none;
      cursor: pointer;
      padding: 24px 48px 24px 0;
      position: relative;
      font-family: var(--font-display);
      font-size: 1.22rem;
      font-weight: 950;
      letter-spacing: -0.035em;
    }

    summary::-webkit-details-marker { display: none; }

    summary::after {
      content: "+";
      position: absolute;
      right: 4px;
      top: 50%;
      width: 32px;
      height: 32px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      transform: translateY(-50%);
      border: 2px solid var(--brown);
      background: white;
      font-family: var(--font-body);
      font-size: 1.25rem;
    }

    details[open] summary::after { content: "-"; background: var(--banana); }

    details p {
      max-width: 720px;
      padding: 0 48px 24px 0;
      color: rgba(48,37,31,0.72);
    }

    footer {
      margin-top: 70px;
      padding: 70px 0 30px;
      background: var(--brown);
      color: white;
    }

    .footer-grid {
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap: 50px;
      padding-bottom: 48px;
      border-bottom: 1px solid rgba(255,255,255,0.2);
    }

    .footer-brand .brand { color: white; margin-bottom: 18px; }
    .footer-brand p { max-width: 470px; color: rgba(255,255,255,0.65); }

    .footer-links {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 24px;
    }

    .footer-links h3 {
      margin-bottom: 15px;
      font-size: 1rem;
      letter-spacing: 0.02em;
    }

    .footer-links a {
      display: block;
      margin: 8px 0;
      color: rgba(255,255,255,0.7);
      text-decoration: none;
      font-size: 0.91rem;
    }

    .footer-bottom {
      display: flex;
      justify-content: space-between;
      gap: 20px;
      padding-top: 24px;
      color: rgba(255,255,255,0.54);
      font-size: 0.78rem;
    }

    .drawer-backdrop,
    .modal-backdrop {
      position: fixed;
      inset: 0;
      z-index: 80;
      display: none;
      background: rgba(40,25,17,0.44);
      backdrop-filter: blur(4px);
    }

    .drawer-backdrop.open,
    .modal-backdrop.open { display: block; }

    .cart-drawer {
      position: absolute;
      top: 0;
      right: 0;
      width: min(440px, 100%);
      height: 100%;
      padding: 26px;
      background: var(--paper);
      box-shadow: -20px 0 60px rgba(40,25,17,0.24);
      transform: translateX(100%);
      transition: transform 220ms ease;
    }

    .drawer-backdrop.open .cart-drawer { transform: translateX(0); }

    .drawer-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 20px;
      padding-bottom: 20px;
      border-bottom: 2px solid var(--brown);
    }

    .drawer-head h2 { margin: 0; font-size: 2rem; }

    .close-button {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      border: 2px solid var(--brown);
      background: white;
      cursor: pointer;
      font-size: 1.25rem;
      font-weight: 950;
    }

    .cart-item {
      display: grid;
      grid-template-columns: 90px 1fr auto;
      gap: 14px;
      padding: 22px 0;
      border-bottom: 1px solid var(--line);
    }

    .cart-thumb {
      width: 90px;
      height: 100px;
      display: grid;
      place-items: center;
      overflow: hidden;
      border: 2px solid var(--brown);
      border-radius: 18px;
      background: var(--banana);
    }

    .cart-item h3 { margin: 3px 0 6px; font-size: 1.05rem; line-height: 1.1; }
    .cart-meta { color: rgba(48,37,31,0.64); font-size: 0.82rem; }
    .cart-item-price { font-weight: 950; }

    .cart-empty {
      padding: 55px 12px;
      text-align: center;
      color: rgba(48,37,31,0.64);
    }

    .drawer-footer {
      position: absolute;
      left: 26px;
      right: 26px;
      bottom: 26px;
      padding-top: 18px;
      border-top: 2px solid var(--brown);
      background: var(--paper);
    }

    .total-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 14px;
      font-weight: 950;
    }

    .checkout-button {
      width: 100%;
      min-height: 55px;
      border: 2px solid var(--brown);
      border-radius: 999px;
      background: var(--coral);
      color: white;
      box-shadow: 4px 4px 0 var(--brown);
      cursor: pointer;
      font-weight: 950;
    }

    .modal {
      width: min(520px, calc(100% - 30px));
      position: absolute;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%);
      padding: 28px;
      border: 3px solid var(--brown);
      border-radius: 32px;
      background: var(--paper);
      box-shadow: 10px 10px 0 var(--brown);
    }

    .modal h2 { margin: 0 0 12px; font-size: 2.5rem; }
    .modal p { color: rgba(48,37,31,0.68); }

    .share-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 10px;
      margin-top: 20px;
    }

    .share-chip {
      min-height: 50px;
      border: 2px solid var(--brown);
      border-radius: 16px;
      background: white;
      color: var(--brown);
      font-weight: 900;
    }

    .toast {
      position: fixed;
      left: 50%;
      bottom: 28px;
      z-i