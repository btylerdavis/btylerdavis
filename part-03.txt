 {
      display: grid;
      grid-template-columns: minmax(0, 1.05fr) minmax(390px, 0.95fr);
      gap: 32px;
      padding: 30px;
      border: 2px solid var(--brown);
      border-radius: 44px;
      background: white;
      box-shadow: var(--shadow);
    }

    .gallery {
      display: grid;
      grid-template-columns: 82px 1fr;
      gap: 16px;
      min-height: 610px;
    }

    .thumbs {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .thumb-button {
      width: 82px;
      height: 82px;
      padding: 7px;
      border: 2px solid var(--line);
      border-radius: 20px;
      background: var(--cream);
      overflow: hidden;
    }

    .thumb-button.active {
      border-color: var(--brown);
      box-shadow: 3px 3px 0 var(--brown);
    }

    .gallery-main {
      position: relative;
      display: grid;
      place-items: center;
      min-height: 610px;
      border-radius: 34px;
      overflow: hidden;
      background:
        radial-gradient(circle at 50% 40%, rgba(255,255,255,0.75) 0 145px, transparent 146px),
        var(--banana);
      border: 2px solid var(--brown);
    }

    .gallery-main::after {
      content: "";
      position: absolute;
      inset: 0;
      background-image: radial-gradient(rgba(75, 45, 32, 0.12) 1.4px, transparent 1.4px);
      background-size: 18px 18px;
      pointer-events: none;
    }

    .gallery-view {
      display: none;
      width: 100%;
      height: 100%;
      position: relative;
      z-index: 1;
      place-items: center;
    }

    .gallery-view.active { display: grid; }

    .gallery-view svg { width: 92%; max-height: 590px; }

    .zoom-note {
      position: absolute;
      left: 18px;
      bottom: 16px;
      z-index: 3;
      padding: 8px 12px;
      border-radius: 999px;
      background: rgba(255,255,255,0.9);
      border: 1px solid rgba(75,45,32,0.18);
      font-size: 0.82rem;
      font-weight: 850;
    }

    .product-info { padding: 18px 10px 12px 18px; }

    .product-badges {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 17px;
    }

    .product-badge {
      padding: 7px 11px;
      border-radius: 999px;
      background: #fff3c2;
      border: 1px solid rgba(75,45,32,0.14);
      color: var(--brown);
      font-size: 0.78rem;
      font-weight: 900;
    }

    .product-info h2 {
      margin-bottom: 15px;
      font-size: clamp(2.5rem, 4vw, 4.2rem);
    }

    .product-price-row {
      display: flex;
      align-items: baseline;
      gap: 12px;
      margin-bottom: 20px;
    }

    .product-price {
      font-family: var(--font-display);
      font-size: 2.1rem;
      font-weight: 950;
    }

    .price-note { color: rgba(48, 37, 31, 0.58); font-weight: 750; }

    .product-description {
      color: rgba(48, 37, 31, 0.74);
      font-size: 1.04rem;
      margin-bottom: 26px;
    }

    .feature-list {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
      margin: 0 0 27px;
      padding: 0;
      list-style: none;
    }

    .feature-list li {
      display: flex;
      align-items: flex-start;
      gap: 9px;
      font-size: 0.91rem;
      font-weight: 800;
    }

    .feature-dot {
      flex: 0 0 auto;
      width: 24px;
      height: 24px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      background: #eaf8ed;
      color: #27815a;
      font-size: 0.73rem;
    }

    .option-label {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      margin-bottom: 10px;
      font-weight: 950;
    }

    .option-label a {
      color: var(--coral-deep);
      font-size: 0.83rem;
      font-weight: 850;
    }

    .size-grid {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      gap: 8px;
      margin-bottom: 22px;
    }

    .size-button {
      min-height: 46px;
      border-radius: 14px;
      border: 2px solid var(--line);
      background: white;
      color: var(--brown);
      font-weight: 950;
    }

    .size-button.selected {
      border-color: var(--brown);
      background: var(--banana);
      box-shadow: 3px 3px 0 var(--brown);
    }

    .purchase-row {
      display: grid;
      grid-template-columns: 110px 1fr;
      gap: 12px;
      margin-bottom: 13px;
    }

    .quantity {
      display: grid;
      grid-template-columns: 36px 1fr 36px;
      align-items: center;
      min-height: 58px;
      border: 2px solid var(--brown);
      border-radius: 999px;
      background: white;
      overflow: hidden;
    }

    .quantity button {
      height: 100%;
      border: 0;
      background: transparent;
      color: var(--brown);
      cursor: pointer;
      font-size: 1.25rem;
      font-weight: 950;
    }

    .quantity output { text-align: center; font-weight: 950; }

    .add-button {
      min-height: 58px;
      border: 2px solid var(--brown);
      border-radius: 999px;
      background: var(--coral);
      color: white;
      box-shadow: 5px 5px 0 var(--brown);
      cursor: pointer;
      font-weight: 950;
    }

    .add-button:hover { transform: translateY(-2px); box-shadow: 3px 3px 0 var(--brown); }

    .share-product-button {
      width: 100%;
      min-height: 52px;
      border: 2px solid var(--brown);
      border-radius: 999px;
      background: #e7f9fd;
      color: var(--brown);
      cursor: pointer;
      font-weight: 950;
    }

    .mini-note {
      margin: 17px 0 0;
      color: rgba(48, 37, 31, 0.57);
      font-size: 0.78rem;
      line-height: 1.45;
    }

    .difference {
      background: var(--brown);
      color: white;
      position: relative;
      overflow: hidden;
    }

    .difference::before,
    .difference::after {
      content: "";
      position: absolute;
      border-radius: 50%;
      background: var(--banana);
      opacity: 0.1;
    }

    .difference::before { width: 310px; height: 310px; left: -140px; top: -120px; }
    .difference::after { width: 410px; height: 410px; right: -190px; bottom: -190px; }

    .difference .section-heading { position: relative; z-index: 1; }
    .difference .section-heading p { color: rgba(255,255,255,0.67); }
    .difference .section-kicker { color: var(--banana); }

    .difference-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 18px;
      position: relative;
      z-index: 1;
    }

    .difference-card {
      min-height: 310px;
      padding: 28px;
      border: 2px solid rgba(255,255,255,0.38);
      border-radius: 30px;
      background: rgba(255,255,255,0.07);
      backdrop-filter: blur(6px);
    }

    .difference-card:nth-child(2) { transform: translateY(20px); }

    .number-pill {
      width: 52px;
      height: 52px;
      border-radius: 18px;
      display: grid;
      place-items: center;
      border: 2px solid white;
      background: var(--coral);
      box-shadow: 4px 4px 0 rgba(255,255,255,0.28);
      font-family: var(--font-display);
      font-size: 1.28rem;
      font-weight: 950;
    }

    .difference-card h3 {
      margin: 25px 0 12px;
      color: white;
      font-size: 2rem;
    }

    .difference-card p { color: rgba(255,255,255,0.7); margin-bottom: 0; }

    .gift-section { padding-bottom: 60px; }

    .gift-card {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(330px, 0.72fr);
      align-items: center;
      gap: 36px;
      padding: clamp(34px, 6vw, 72px);
      border: 3px solid var(--brown);
