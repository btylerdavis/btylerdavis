/**
 * DUA-gate constants shared by the server actions and the dashboard's
 * cookie check (kept out of actions.ts — a "use server" module may only
 * export async functions).
 */
export const DUA_COOKIE = "sleeptopia_partner_dua";
export const DUA_VERSION = "dua-demo-v1";
