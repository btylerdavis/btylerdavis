<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Tot Onesie | The $20 Back-Zip Monkey Sleeper</title>
  <meta name="description" content="Meet Tot Onesie: a playful $20 infant sleeper with an original monkey print and a clever back zipper. Made to gift, share, and cuddle.">
  <meta name="theme-color" content="#ffd447">
  <meta property="og:type" content="product">
  <meta property="og:title" content="Tot Onesie | Back-Zip Bedtime. Front-Row Cuddles.">
  <meta property="og:description" content="The playful $20 monkey sleeper with a clever zipper in back.">
  <meta property="og:image" content="social-card.png">
  <meta property="product:price:amount" content="20.00">
  <meta property="product:price:currency" content="USD">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="Tot Onesie | The $20 Back-Zip Monkey Sleeper">
  <meta name="twitter:description" content="Big personality. Tiny package. One clever zipper in back.">
  <link rel="icon" href="favicon.svg" type="image/svg+xml">
  <link rel="manifest" href="manifest.webmanifest">
  <style>
    :root {
      --ink: #30251f;
      --brown: #4b2d20;
      --cream: #fff9ea;
      --paper: #fffdf6;
      --banana: #ffd447;
      --banana-deep: #f3bc19;
      --coral: #ff6d5f;
      --coral-deep: #e84f45;
      --sky: #62cce7;
      --leaf: #49ba86;
      --lavender: #a58bed;
      --line: rgba(75, 45, 32, 0.15);
      --shadow: 0 24px 70px rgba(75, 45, 32, 0.15);
      --shadow-soft: 0 12px 32px rgba(75, 45, 32, 0.10);
      --radius-lg: 36px;
      --radius-md: 24px;
      --radius-sm: 16px;
      --font-display: "Arial Rounded MT Bold", "Avenir Next", "Trebuchet MS", system-ui, sans-serif;
      --font-body: "Avenir Next", "Trebuchet MS", system-ui, sans-serif;
    }

    * { box-sizing: border-box; }

    html { scroll-behavior: smooth; }

    body {
      margin: 0;
      color: var(--ink);
      background:
        radial-gradient(circle at 8% 9%, rgba(255, 212, 71, 0.24) 0 115px, transparent 116px),
        radial-gradient(circle at 92% 17%, rgba(98, 204, 231, 0.18) 0 150px, transparent 151px),
        linear-gradient(180deg, var(--paper), #fffaf0 44%, #fffdf8 100%);
      font-family: var(--font-body);
      line-height: 1.55;
      overflow-x: hidden;
    }

    button, select, input { font: inherit; }

    button, a { -webkit-tap-highlight-color: transparent; }

    a { color: inherit; }

    img, svg { display: block; max-width: 100%; }

    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      border: 0;
    }

    .announcement {
      min-height: 42px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      padding: 8px 20px;
      background: var(--brown);
      color: #fff9e9;
      text-align: center;
      font-size: 0.9rem;
      font-weight: 800;
      letter-spacing: 0.02em;
    }

    .announcement .spark { color: var(--banana); }

    .shell {
      width: min(1180px, calc(100% - 36px));
      margin: 0 auto;
    }

    .nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 28px;
      min-height: 84px;
      position: relative;
      z-index: 20;
    }

    .brand {
      display: inline-flex;
      align-items: center;
      gap: 11px;
      text-decoration: none;
      font-family: var(--font-display);
      font-size: 1.26rem;
      font-weight: 900;
      letter-spacing: -0.04em;
    }

    .brand-mark {
      width: 44px;
      height: 44px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      background: var(--banana);
      border: 2px solid var(--brown);
      box-shadow: 4px 4px 0 var(--brown);
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 28px;
      font-weight: 800;
      font-size: 0.95rem;
    }

    .nav-links a {
      text-decoration: none;
      position: relative;
    }

    .nav-links a::after {
      content: "";
      position: absolute;
      left: 0;
      right: 100%;
      bottom: -5px;
      height: 3px;
      border-radius: 999px;
      background: var(--coral);
      transition: right 180ms ease;
    }

    .nav-links a:hover::after,
    .nav-links a:focus-visible::after { right: 0; }

    .nav-actions { display: flex; align-items: center; gap: 10px; }

    .icon-button,
    .pill-button,
    .primary-button,
    .secondary-button,
    .size-button,
    .share-chip,
    .thumb-button {
      border: 0;
      cursor: pointer;
      transition: transform 160ms ease, box-shadow 160ms ease, background 160ms ease;
    }

    .icon-button:hover,
    .pill-button:hover,
    .primary-button:hover,
    .secondary-button:hover,
    .size-button:hover,
    .share-chip:hover,
    .thumb-button:hover { transform: translateY(-2px); }

    .icon-button:focus-visible,
    .pill-button:focus-visible,
    .primary-button:focus-visible,
    .secondary-button:focus-visible,
    .size-button:focus-visible,
    .share-chip:focus-visible,
    .thumb-button:focus-visible,
    a:focus-visible,
    select:focus-visible,
    input:focus-visible {
      outline: 4px solid rgba(98, 204, 231, 0.4);
      outline-offset: 3px;
    }

    .cart-button {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      min-height: 46px;
      padding: 0 16px;
      border-radius: 999px;
      border: 2px solid var(--brown);
      background: white;
      color: var(--brown);
      font-weight: 900;
      box-shadow: 3px 3px 0 var(--brown);
    }

    .cart-count {
      min-width: 23px;
      height: 23px;
      padding: 0 6px;
      border-radius: 999px;
      display: inline-grid;
      place-items: center;
      background: var(--coral);
      color: white;
      font-size: 0.76rem;
    }

    .hero {
      display: grid;
      grid-template-columns: minmax(0, 1.02fr) minmax(430px, 0.98fr);
      align-items: center;
      gap: 56px;
      min-height: 700px;
      padding: 50px 0 88px;
      position: relative;
    }

    .hero-copy { position: relative; z-index: 2; }

    .eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 9px;
      margin: 0 0 22px;
      padding: 9px 14px;
      border: 2px solid var(--brown);
      border-radius: 999px;
      background: #fff;
      color: var(--brown);
      font-size: 0.79rem;
      font-weight: 950;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      box-shadow: 4px 4px 0 rgba(75, 45, 32, 0.14);
    }

    .eyebrow-dot {
      width: 9px;
      height: 9px;
      border-radius: 50%;
      background: var(--leaf);
      box-shadow: 0 0 0 5px rgba(73, 186, 134, 0.17);
    }

    h1, h2, h3, p { margin-top: 0; }

    h1, h2, h3 {
      font-family: var(--font-display);
      letter-spacing: -0.055em;
      line-height: 0.98;
    }

    h1 {
      max-width: 720px;
      margin-bottom: 24px;
      font-size: clamp(3.8rem, 6.2vw, 5.9rem);
      font-weight: 950;
    }

    .headline-pop {
      display: block;
      position: relative;
      width: fit-content;
      max-width: 100%;
      margin-top: 0.08em;
      color: var(--coral-deep);
      font-size: 0.72em;
      line-height: 1.02;
      white-space: normal;
    }

    .headline-pop::after {
      content: "";
      position: absolute;
